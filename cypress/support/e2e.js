import "./commands";
import "../e2e/features/login.feature";
import "../e2e/features/cart.feature";
beforeEach(() => {
  cy.log("Iniciando prueba..."); // Mensaje que se ejecuta antes de cada prueba
});
beforeEach(() => {
  cy.log("Iniciando prueba..."); // Mensaje que se ejecuta antes de cada prueba
});
