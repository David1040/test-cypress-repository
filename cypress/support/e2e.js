
beforeEach(() => {
  cy.log("Iniciando prueba..."); // Mensaje que se ejecuta antes de cada prueba
});
beforeEach(() => {
  cy.log("Iniciando prueba..."); // Mensaje que se ejecuta antes de cada prueba
});
